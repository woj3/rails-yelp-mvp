<div class="container">
  <h1 class="py-5">Add a new Restaurant</h1>

  <%= simple_form_for(@restaurant) do |f| %>
    <%= f.input :name %>
    <%= f.input :address %>
    <%= f.input :category %>
    <%= f.button :submit, class: "btn btn-primary"%>
  <% end %>
</div>
